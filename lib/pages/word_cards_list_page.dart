import 'package:flutter/material.dart';

class WordCardsListPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('単語カード'),
        centerTitle: true,
      ),
    );
  }
}